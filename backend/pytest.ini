[pytest]
markers =
    eval: model evals — real LLM calls against a seeded fixture DB (opt-in: pytest -m eval)
; evals are excluded by default; an explicit `-m eval` on the CLI overrides this
addopts = -m "not eval"
